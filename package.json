{
  "name": "iZph",
  "version": "1.0.0",
  "author": "iZph",
  "scripts": {
    "dev": "vuepress dev . --open --host \"localhost\"",
    "start": "vuepress dev . --open --host \"localhost\"",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && rimraf ./public && vuepress build .",
    "clean": "rimraf ./public",
    "bash": "npm run clean && bash deploy.sh",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "vue-template-compiler": "^2.6.14",
    "vuepress": "1.8.2",
    "vuepress-theme-reco": "1.6.10"
  },
  "description": "",
  "dependencies": {
    "markdown-it-disable-url-encode": "^1.0.1",
    "vuepress-plugin-auto-sidebar": "^2.3.2"
  }
}